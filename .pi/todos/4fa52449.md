{
  "id": "4fa52449",
  "title": "P3.4: Component tests for ported SvelteKit pages",
  "tags": [],
  "status": "completed",
  "created_at": "2026-07-09T01:49:30.945Z",
  "assigned_to_session": "019f448a-85a1-73ee-8c11-16c0e8e3ce91"
}

Add @testing-library/svelte component tests for ported SvelteKit pages.

Ported pages (web/src/routes/):
- /about, /terms, /atproto (static)
- /join/create (signup categories)
- /my-coffee (manage + brew list)
- / (home/feed)
- /beans/[actor]/[id], /roasters/[actor]/[id], /grinders/[actor]/[id], /brewers/[actor]/[id], /brews/[actor]/[id], /recipes/[actor]/[id] (entity views)

For each ported page:
- Test rendering with mocked API responses
- Test key interactions (form submit, filter change, pagination, like)
- Test error states (API 401, 500, network error)

Target: every ported page has at least one component test. Location: web/src/routes/**/*.test.ts, using vitest + @testing-library/svelte (already configured in web/vite.config.ts).
