{
  "id": "7f631d29",
  "title": "P3.5: Snapshot tests for API JSON responses",
  "tags": [],
  "status": "completed",
  "created_at": "2026-07-09T01:49:37.325Z",
  "assigned_to_session": "019f448a-85a1-73ee-8c11-16c0e8e3ce91"
}

Add JSON snapshot tests for key API endpoints using the existing `shutter` library.

Endpoints to snapshot:
- GET /api/feed (feed JSON envelope shape)
- GET /api/{entity}/{actor}/{id} (entity view JSON shape) — at least bean + brew
- GET /api/profile/{actor} (profile data bundle shape)

Pattern: follow tests/integration/snapshot_test.go — use scrubPDS() to replace DIDs, rkeys, timestamps with stable placeholders. Snapshots stored in tests/integration/__snapshots__/.

These catch unintended shape changes during refactors.
