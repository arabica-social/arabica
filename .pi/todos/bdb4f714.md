{
  "id": "bdb4f714",
  "title": "Remove dead HTMX remnants in SPA (modal-container, editModalURL)",
  "tags": [],
  "status": "completed",
  "created_at": "2026-07-10T03:29:55.182Z"
}

Cleanup of dead HTMX remnants in the SPA:
1. Remove `<div id="modal-container"></div>` from web/src/routes/+layout.svelte:109 — nothing writes to it in the SPA (legacy ModalContainerIsland isn't loaded).
2. Remove dead `editModalURL` prop chain: web/src/lib/components/ActionBar.svelte (lines 17, 40, 281-282), web/src/lib/components/EntityViewLayout.svelte (lines 31, 64, 120), web/src/lib/components/FeedCard.svelte:84. Always "" — edit goes to full pages via editURL.
3. Update stale HTMX comments in web/tests/e2e/view-feed.spec.ts (lines 8-16, 25-27) that claim the feed uses HTMX — it now uses client-side fetch JSON.
Low priority; do after functional fixes.
